import { ArgsTable } from '@storybook/addon-docs'
import { ColorBlock } from './index'

# Usage

Get the color variable name and use it as a custom property in the format of `var(--color-<name>)`, e.g.:

```css
color: var(--color-yellow);
color: var(--color-text-gray-3);
```

# Arguments

<ArgsTable of={ColorBlock} />
